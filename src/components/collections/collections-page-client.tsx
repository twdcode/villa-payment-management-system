"use client";

import { AlertTriangle, CalendarDays, CheckCircle2, ChevronDown, Clock3, Plus, Search, ShieldCheck } from "lucide-react";
import Link from "next/link";
import { useEffect, useMemo, useState } from "react";

import { AppShell } from "@/components/layout/app-shell";
import { CollectionRowActions } from "@/components/collections/collection-row-actions";
import { RecordPaymentDialog } from "@/components/collections/record-payment-dialog";
import { ReminderReviewDialog } from "@/components/collections/reminder-review-dialog";
import { Button } from "@/components/ui/button";
import type { Collection, MockDatabase, ReminderApproval, ReminderApprovalStatus } from "@/lib/domain/types";
import { collectionStatusLabels } from "@/lib/domain/status-labels";
import { DEMO_TODAY } from "@/lib/config/demo";
import { addDays, calculateVillaFinancials, isPaymentScheduleReady, paymentStatus, principalOutstanding } from "@/lib/finance/calculations";
import { formatLkr } from "@/lib/formatters";
import { mockRepository } from "@/lib/repositories/local-storage-repository";

type View = "all" | "reminders";

const collectionStatusClasses = { confirmed: "bg-success/10 text-success", reversed: "bg-danger/10 text-danger" };
const approvalLabels: Record<ReminderApprovalStatus, string> = { awaiting_approval: "Awaiting approval", cancelled: "Cancelled", ready_to_send: "Ready to send", sent: "Sent" };
const approvalClasses: Record<ReminderApprovalStatus, string> = { awaiting_approval: "bg-warning/15 text-warning", cancelled: "bg-danger/10 text-danger", ready_to_send: "bg-surface-muted text-primary", sent: "bg-success/10 text-success" };
const formatDate = (value: string) => new Intl.DateTimeFormat("en-LK", { day: "2-digit", month: "short", year: "numeric" }).format(new Date(`${value}T00:00:00`));
const formatDateTime = (value: string) => new Intl.DateTimeFormat("en-LK", { day: "numeric", month: "short", year: "numeric", hour: "numeric", minute: "2-digit" }).format(new Date(value));

function SelectControl({ children, onChange, value }: { children: React.ReactNode; onChange: (value: string) => void; value: string }) {
  return <label className="relative block"><select className="h-12 w-full appearance-none rounded-md border bg-surface px-4 pr-10 text-sm font-semibold text-foreground outline-none focus-visible:ring-2 focus-visible:ring-ring" onChange={(event) => onChange(event.target.value)} value={value}>{children}</select><ChevronDown aria-hidden="true" className="pointer-events-none absolute right-4 top-1/2 size-5 -translate-y-1/2 text-muted-foreground" /></label>;
}

function CollectionTable({ collections, database }: { collections: Collection[]; database: MockDatabase }) {
  const customers = new Map(database.customers.map((customer) => [customer.id, customer]));
  const villas = new Map(database.villas.map((villa) => [villa.id, villa]));
  const projects = new Map(database.projects.map((project) => [project.id, project]));
  const receipts = new Map(database.receipts.map((receipt) => [receipt.collectionId, receipt]));
  return <div className="overflow-x-auto rounded-lg border bg-surface"><table className="min-w-[75rem] w-full text-left text-sm"><thead className="bg-surface-subtle text-xs font-semibold uppercase tracking-[0.12em] text-muted-foreground"><tr><th className="px-5 py-4">Date</th><th className="px-5 py-4">Receipt</th><th className="px-5 py-4">Customer</th><th className="px-5 py-4">Villa</th><th className="px-5 py-4">Method</th><th className="px-5 py-4">Principal (LKR)</th><th className="px-5 py-4">Interest (LKR)</th><th className="px-5 py-4">Total (LKR)</th><th className="px-5 py-4">Status</th><th className="sticky right-0 z-10 bg-surface-subtle px-5 py-4 text-right">Action</th></tr></thead><tbody>{collections.length ? collections.map((collection) => { const customer = customers.get(collection.customerId); const villa = villas.get(collection.villaId); const project = projects.get(collection.projectId); const receipt = receipts.get(collection.id); return <tr className="border-t" key={collection.id}><td className="whitespace-nowrap px-5 py-4">{formatDate(collection.paymentDate)}</td><td className="px-5 py-4"><p className="font-semibold">{receipt?.number ?? "Receipt pending"}</p><p className="mt-1 text-xs text-muted-foreground">{collection.referenceNumber}</p></td><td className="px-5 py-4 font-medium">{customer?.fullName ?? "Unknown customer"}</td><td className="px-5 py-4"><p className="font-medium">{villa?.number.replace(/^[A-Z]+-/, "Villa ") ?? "Unknown villa"}</p><p className="mt-1 text-xs text-muted-foreground">{project?.location ?? ""}</p></td><td className="px-5 py-4 capitalize">{collection.paymentMethod.replace("_", " ")}</td><td className="px-5 py-4 font-semibold">{formatLkr(collection.principalAmount)}</td><td className="px-5 py-4 font-semibold">{formatLkr(collection.interestAmount)}</td><td className="px-5 py-4 font-semibold">{formatLkr(collection.totalAmount)}</td><td className="px-5 py-4"><span className={`inline-flex rounded-full px-3 py-1 text-xs font-semibold ${collectionStatusClasses[collection.status]}`}>{collectionStatusLabels[collection.status]}</span></td><td className="sticky right-0 z-10 bg-surface px-5 py-4 text-right"><CollectionRowActions collection={collection} database={database} /></td></tr>; }) : <tr><td className="px-5 py-10 text-center text-muted-foreground" colSpan={10}>No collections match these filters.</td></tr>}</tbody></table></div>;
}

function ReminderTable({ approvals, database, onReview }: { approvals: ReminderApproval[]; database: MockDatabase; onReview: (approval: ReminderApproval) => void }) {
  const customers = new Map(database.customers.map((customer) => [customer.id, customer]));
  const villas = new Map(database.villas.map((villa) => [villa.id, villa]));
  const projects = new Map(database.projects.map((project) => [project.id, project]));
  const users = new Map(database.users.map((user) => [user.id, user]));
  return <div className="overflow-x-auto rounded-lg border bg-surface"><table className="min-w-[68rem] w-full text-left text-sm"><thead className="bg-surface-subtle text-xs font-semibold uppercase tracking-[0.12em] text-muted-foreground"><tr><th className="px-5 py-4">Customer</th><th className="px-5 py-4">Villa</th><th className="px-5 py-4">Send date</th><th className="px-5 py-4">Requested by</th><th className="px-5 py-4">Status</th><th className="px-5 py-4">Villa</th><th className="sticky right-0 z-10 bg-surface-subtle px-5 py-4 text-right">Action</th></tr></thead><tbody>{approvals.length ? approvals.map((approval) => { const customer = customers.get(approval.customerId); const villa = villas.get(approval.villaId); const project = villa ? projects.get(villa.projectId) : null; const requester = approval.requestedBy ? users.get(approval.requestedBy) : null; return <tr className="border-t" key={approval.id}><td className="px-5 py-4 font-medium">{customer?.fullName ?? "Unknown customer"}</td><td className="px-5 py-4"><p className="font-medium">{villa?.number.replace(/^[A-Z]+-/, "Villa ") ?? "Unknown villa"}</p><p className="mt-1 text-xs text-muted-foreground">{project?.location ?? ""}</p></td><td className="whitespace-nowrap px-5 py-4">{formatDate(approval.sendDate)}</td><td className="px-5 py-4"><p className="font-semibold">{requester?.name ?? "System generated"}</p><p className="mt-1 whitespace-nowrap text-xs text-muted-foreground">{formatDateTime(approval.requestedAt)}</p></td><td className="px-5 py-4"><span className={`inline-flex rounded-full px-3 py-1 text-xs font-semibold ${approvalClasses[approval.status]}`}>{approvalLabels[approval.status]}</span></td><td className="px-5 py-4"><Link className="font-semibold hover:text-muted-foreground" href={`/projects/${villa?.projectId}/villas/${villa?.id}`}>View villa</Link></td><td className="sticky right-0 z-10 bg-surface px-5 py-4 text-right"><Button disabled={approval.status === "cancelled" || approval.status === "sent"} aria-label={`Review reminder for ${customer?.fullName ?? "customer"}`} onClick={() => onReview(approval)} variant="outline">Review</Button></td></tr>; }) : <tr><td className="px-5 py-10 text-center text-muted-foreground" colSpan={7}>No reminders match these filters.</td></tr>}</tbody></table></div>;
}

function CollectionSummaryCards({ database }: { database: MockDatabase }) {
  const summary = useMemo(() => {
    const activeVillas = database.villas.filter((villa) => villa.operationalStatus !== "cancelled");
    const villaMap = new Map(activeVillas.map((villa) => [villa.id, villa]));
    const collectionsThisMonth = database.collections.filter((collection) => villaMap.has(collection.villaId) && collection.status === "confirmed" && collection.paymentDate.startsWith(DEMO_TODAY.slice(0, 7)));
    const schedules = database.schedules.flatMap((schedule) => villaMap.has(schedule.villaId) ? [schedule] : []);
    const dueWithinSixtyDays = schedules.filter((schedule) => isPaymentScheduleReady(schedule) && schedule.dueDate >= DEMO_TODAY && schedule.dueDate <= addDays(DEMO_TODAY, 60) && principalOutstanding(schedule) > 0);
    const overdue = schedules.filter((schedule) => paymentStatus(schedule, DEMO_TODAY) === "overdue" && principalOutstanding(schedule) > 0);
    const interest = activeVillas.reduce((total, villa) => {
      const terms = { ...database.settings.defaultInterestTerms, ...villa.interestTerms, ...(villa.chargeLatePaymentInterest === false ? { monthlyRate: 0 } : {}) };
      return total + calculateVillaFinancials(database.schedules.filter((schedule) => schedule.villaId === villa.id), terms, DEMO_TODAY).interestOutstanding;
    }, 0);
    return { collected: { value: collectionsThisMonth.reduce((total, collection) => total + collection.totalAmount, 0), count: collectionsThisMonth.length }, due: { value: dueWithinSixtyDays.reduce((total, schedule) => total + principalOutstanding(schedule), 0), count: dueWithinSixtyDays.length }, overdue: { value: overdue.reduce((total, schedule) => total + principalOutstanding(schedule), 0), count: overdue.length }, interest };
  }, [database]);
  const cards = [
    { label: "Collected this month", value: summary.collected.value, detail: `${summary.collected.count} ${summary.collected.count === 1 ? "payment" : "payments"} received`, icon: CheckCircle2, iconClass: "bg-success/10 text-success", cardClass: "" },
    { label: "Due in next 60 days", value: summary.due.value, detail: `${summary.due.count} scheduled ${summary.due.count === 1 ? "payment" : "payments"}`, icon: CalendarDays, iconClass: "bg-sky-100 text-sky-700", cardClass: "" },
    { label: "Overdue", value: summary.overdue.value, detail: `${summary.overdue.count} ${summary.overdue.count === 1 ? "requires" : "require"} follow-up`, icon: AlertTriangle, iconClass: "bg-danger/10 text-danger", cardClass: "border-danger/30" },
    { label: "Interest outstanding", value: summary.interest, detail: "Calculated from agreement terms", icon: Clock3, iconClass: "bg-warning/15 text-warning", cardClass: "" },
  ];
  return <section aria-label="Collection summary" className="mt-7 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map((card) => { const Icon = card.icon; return <article className={`rounded-lg border bg-surface p-5 ${card.cardClass}`} key={card.label}><div className="flex items-start gap-4"><span className={`grid size-12 shrink-0 place-items-center rounded-md ${card.iconClass}`}><Icon className="size-6" /></span><div className="min-w-0"><p className="text-sm text-muted-foreground">{card.label}</p><p className="mt-2 text-xl font-semibold">{formatLkr(card.value)}</p><p className="mt-2 text-xs text-muted-foreground">{card.detail}</p></div></div></article>; })}</section>;
}

export function CollectionsPageClient() {
  const [database, setDatabase] = useState<MockDatabase | null>(null); const [view, setView] = useState<View>("all"); const [collectionOpen, setCollectionOpen] = useState(false); const [selectedApproval, setSelectedApproval] = useState<ReminderApproval | null>(null); const [notice, setNotice] = useState(""); const [search, setSearch] = useState(""); const [projectId, setProjectId] = useState("all"); const [customerId, setCustomerId] = useState("all"); const [collectionStatus, setCollectionStatus] = useState("all"); const [approvalStatus, setApprovalStatus] = useState("all"); const [month, setMonth] = useState("all");
  useEffect(() => { void mockRepository.getDatabase().then(setDatabase); }, []);
  const collections = useMemo(() => !database ? [] : database.collections.filter((collection) => { const customer = database.customers.find((candidate) => candidate.id === collection.customerId); const villa = database.villas.find((candidate) => candidate.id === collection.villaId); const matchesSearch = `${customer?.fullName ?? ""} ${villa?.number ?? ""}`.toLowerCase().includes(search.toLowerCase()); return villa?.operationalStatus !== "cancelled" && matchesSearch && (projectId === "all" || collection.projectId === projectId) && (customerId === "all" || collection.customerId === customerId) && (collectionStatus === "all" || collection.status === collectionStatus); }), [collectionStatus, customerId, database, projectId, search]);
  const approvals = useMemo(() => !database ? [] : database.reminderApprovals.filter((approval) => { const customer = database.customers.find((candidate) => candidate.id === approval.customerId); const villa = database.villas.find((candidate) => candidate.id === approval.villaId); const matchesSearch = `${customer?.fullName ?? ""} ${villa?.number ?? ""}`.toLowerCase().includes(search.toLowerCase()); return matchesSearch && (projectId === "all" || villa?.projectId === projectId) && (approvalStatus === "all" || approval.status === approvalStatus) && (month === "all" || approval.sendDate.slice(5, 7) === month); }), [approvalStatus, database, month, projectId, search]);
  if (!database) return <AppShell active="Collections"><p className="text-sm text-muted-foreground">Loading collections...</p></AppShell>;
  const isSuperAdmin = database.users.find((user) => user.id === "user-vishal")?.role === "super_admin";
  return <AppShell active="Collections"><div className="flex flex-col gap-5 sm:flex-row sm:items-end sm:justify-between"><div><h1 className="text-3xl font-semibold">Collections</h1><p className="mt-2 text-muted-foreground">Record, review and reprint customer payments.</p></div><Button onClick={() => setCollectionOpen(true)}>Add collection <Plus className="size-4" /></Button></div>{view === "all" && <CollectionSummaryCards database={database} />}<div className="mt-8 flex flex-wrap gap-3"><Button onClick={() => setView("all")} size="sm" variant={view === "all" ? "default" : "outline"}>All</Button>{isSuperAdmin && <Button onClick={() => setView("reminders")} size="sm" variant={view === "reminders" ? "default" : "outline"}>Reminder approvals</Button>}</div>{view === "reminders" && isSuperAdmin ? <section className="mt-7"><div className="flex flex-col gap-4 lg:flex-row lg:items-end lg:justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.14em] text-accent">Super Admin controls</p><h2 className="mt-3 text-2xl font-medium">Reminder approval queue</h2><p className="mt-2 text-sm text-muted-foreground">Review the final message and send date before anything reaches the customer.</p></div><p className="inline-flex items-center gap-2 rounded-full border bg-surface px-4 py-2 text-xs font-semibold"><ShieldCheck className="size-4" />Manual approval required</p></div><div className="mt-6 flex flex-wrap gap-3">{([['all', 'All'], ['awaiting_approval', 'Awaiting approval'], ['sent', 'Sent'], ['ready_to_send', 'Ready to send'], ['cancelled', 'Cancelled']] as const).map(([status, label]) => <Button key={status} onClick={() => setApprovalStatus(status)} size="sm" variant={approvalStatus === status ? "default" : "outline"}>{label}</Button>)}</div><div className="mt-4 grid gap-3 lg:grid-cols-[minmax(0,1.45fr)_repeat(3,minmax(0,.6fr))]"><label className="relative"><Search aria-hidden="true" className="pointer-events-none absolute left-4 top-1/2 size-5 -translate-y-1/2 text-muted-foreground" /><input className="h-12 w-full rounded-md border bg-surface pl-11 pr-4 text-sm font-medium outline-none placeholder:text-muted-foreground focus-visible:ring-2 focus-visible:ring-ring" onChange={(event) => setSearch(event.target.value)} placeholder="Search villa name or customer name" value={search} /></label><SelectControl onChange={setMonth} value={month}><option value="all">All months</option><option value="08">August</option><option value="09">September</option></SelectControl><SelectControl onChange={setProjectId} value={projectId}><option value="all">All projects</option>{database.projects.map((project) => <option key={project.id} value={project.id}>{project.name}</option>)}</SelectControl><SelectControl onChange={setApprovalStatus} value={approvalStatus}><option value="all">All statuses</option><option value="awaiting_approval">Awaiting approval</option><option value="ready_to_send">Ready to send</option><option value="sent">Sent</option><option value="cancelled">Cancelled</option></SelectControl></div><div className="mt-4"><ReminderTable approvals={approvals} database={database} onReview={setSelectedApproval} /></div></section> : <section className="mt-4"><div className="grid gap-3 lg:grid-cols-[minmax(0,1.5fr)_repeat(3,minmax(0,.6fr))]"><label className="relative"><Search aria-hidden="true" className="pointer-events-none absolute left-4 top-1/2 size-5 -translate-y-1/2 text-muted-foreground" /><input className="h-12 w-full rounded-md border bg-surface pl-11 pr-4 text-sm font-medium outline-none placeholder:text-muted-foreground focus-visible:ring-2 focus-visible:ring-ring" onChange={(event) => setSearch(event.target.value)} placeholder="Search villa name or customer name" value={search} /></label><SelectControl onChange={setProjectId} value={projectId}><option value="all">All projects</option>{database.projects.map((project) => <option key={project.id} value={project.id}>{project.name}</option>)}</SelectControl><SelectControl onChange={setCustomerId} value={customerId}><option value="all">All customers</option>{database.customers.map((customer) => <option key={customer.id} value={customer.id}>{customer.fullName}</option>)}</SelectControl><SelectControl onChange={setCollectionStatus} value={collectionStatus}><option value="all">All statuses</option><option value="confirmed">Confirmed</option><option value="reversed">Reversed</option></SelectControl></div><div className="mt-4"><CollectionTable collections={collections} database={database} /></div></section>}{collectionOpen && <RecordPaymentDialog database={database} onClose={() => setCollectionOpen(false)} onSuccess={(message) => { setCollectionOpen(false); setNotice(message); void mockRepository.getDatabase().then(setDatabase); }} />}{selectedApproval && <ReminderReviewDialog approval={selectedApproval} database={database} onClose={() => setSelectedApproval(null)} onSuccess={(message) => { setSelectedApproval(null); setNotice(message); void mockRepository.getDatabase().then(setDatabase); }} />}{notice && <div className="fixed right-4 top-24 z-[60] flex max-w-[calc(100vw-2rem)] items-center gap-3 rounded-lg border border-success bg-success px-4 py-3 text-sm font-semibold text-white shadow-lg" role="status"><span>{notice}</span><button aria-label="Dismiss success message" className="rounded-md p-1 hover:bg-white/15" onClick={() => setNotice("")}>x</button></div>}</AppShell>;
}
